class User:
    def __init__(self, name_user, money_user, age_user):
        self.name_user = []
        self.money_user = []
        self.age_user = []
    def search_user(name_user, age_user):
        users = []
        for user in users:
            if name_user and age_user in user:
                print(user)